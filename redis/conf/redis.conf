# Redis 기본 설정
bind 0.0.0.0
port 6379
requirepass pwpw

# 메모리 관리 (금융 데이터 캐싱 고려)
maxmemory 512mb
maxmemory-policy allkeys-lru

# 데이터 영속성 (AOF 활성화)
appendonly yes
appendfilename "appendonly.aof"
appendfsync everysec

# 성능 최적화
tcp-keepalive 300
